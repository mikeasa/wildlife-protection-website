<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lecheng.protectAnimals.dao.SearchDao">
    <insert id="addSearch" parameterType="Search">
        insert into protect_animal.search(userid, searchcontext) values
        (#{userid},#{searchcontext})
    </insert>

    <select id="getCurrentSearches" resultType="string">
        select distinct (protect_animal.search.searchcontext) from protect_animal.search
        where search.userid=#{userid}
        order by updatetime DESC
        limit 3
    </select>

    <select id="getHotSearches" resultType="string">
        SELECT search.searchcontext FROM protect_animal.search
        GROUP BY searchcontext
        ORDER BY COUNT( * ) DESC
        LIMIT 3
    </select>
</mapper>