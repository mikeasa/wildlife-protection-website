<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lecheng.protectAnimals.dao.RoleDao">

    <select id="findRoleListByUserId" resultType="string" parameterType="int">
        SELECT r.rolename FROM protect_animal.role r
        INNER JOIN protect_animal.user_role ur ON ur.`roleid` = r.`roleid`
        WHERE ur.`userid` = #{userid}
    </select>
</mapper>