<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lecheng.protectAnimals.dao.LatAndLngDao">
    <select id="getAddressInfo" parameterType="LatAndLng" resultType="LatAndLng">
        select * from protect_animal.rescuecenter
        <where>
        <if test="province!=null">
            province=#{province}
        </if>
        <if test="city!=null">
            and city=#{city}
        </if>
        </where>
    </select>

</mapper>