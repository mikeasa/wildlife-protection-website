<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lecheng.protectAnimals.dao.PermissionDao">
    <select id="findPermissionListByUserId" resultType="string">
        SELECT  p.permissioncode FROM protect_animal.permission p
        INNER JOIN protect_animal.role_permission rp ON rp.permissionid = p.permissionid
        INNER JOIN protect_animal.user_role ur ON ur.`roleid` = rp.roleid
        WHERE ur.`userid` = #{userid}
    </select>
</mapper>